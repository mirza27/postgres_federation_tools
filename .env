APP_NAME=migrate_db

# can be updated later
NEW_DATABASE=new_publication_db
NEW_USERNAME=new_user
NEW_PASSWORD=dsav9das
NEW_OUT_PORT=5233

# can be updated later
OLD_DATABASE=old_publication_db
OLD_USERNAME=old_user
OLD_PASSWORD=jdfmhuvdas
OLD_OUT_PORT=5234

PIVOT_DATABASE=pivot
PIVOT_USERNAME=pivot_user
PIVOT_PASSWORD=djsadsaj
PIVOT_OUT_PORT=5435
PIVOT_SCHEMA_PATH=./internal/config/pivot_db.sql

KAFKA_BROKERS=localhost:29092
KAFKA_GROUP_ID=db-migrate-engine
KAFKA_CONSUMER_RESET=true

PIVOT_DSN=postgres://${PIVOT_USERNAME}:${PIVOT_PASSWORD}@localhost:${PIVOT_OUT_PORT}/${PIVOT_DATABASE}?sslmode=disable
TARGET_DSN=postgres://${NEW_USERNAME}:${NEW_PASSWORD}@localhost:${NEW_OUT_PORT}/${NEW_DATABASE}?sslmode=disable
MAPPING_PATH=./internal/config/json/publication
BATCH_MAX_ROWS=1000
BATCH_MAX_INTERVAL=1000
JOIN_WAIT_TTL=120

RUN_API_PORT=5000

